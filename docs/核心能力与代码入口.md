# 核心能力与代码入口

> 只回答一个问题：
> **遇到某个机制问题，第一段代码打开哪里？**

---

# 1. Single-Agent 主链只记 8 个 Owner

```text
Harness.run
↓
AgentLoop.run
↓
RunPreparation.prepare_run
↓
TurnPreparation.prepare_turn
↓
ModelGateway.chat
↓
ToolExecutionPipeline._execute_call
↓
FinalAnswerBuilder.build_stop_request
↓
RunLifecycle.finalize_run
```

| Owner | 只负责什么 | 直接打开 |
|---|---|---|
| `Harness.run` | Public API、运行产物、ExecutionEnvironment、Runtime 装配 | `agent_forge/harness.py` |
| `AgentLoop.run` | 主循环和唯一控制流 | `agent_forge/runtime/application/agent_loop.py` |
| `RunPreparation.prepare_run` | 首轮前 clarification / Skill / Memory / resume seed | `agent_forge/runtime/application/run_preparation.py` |
| `TurnPreparation.prepare_turn` | 当前 Turn 的 LLM input snapshot | `agent_forge/runtime/application/turn_preparation.py` |
| `ModelGateway.chat` | Provider / retry / usage | `agent_forge/runtime/adapters/model_gateway.py` |
| `ToolExecutionPipeline._execute_call` | Tool governance 主链 | `agent_forge/runtime/application/tool_execution.py` |
| `FinalAnswerBuilder` | 模型最终文本 → typed stop candidate | `agent_forge/runtime/application/final_answer.py` |
| `RunLifecycle.finalize_run` | durable final state | `agent_forge/runtime/application/run_lifecycle.py` |

---

# 2. 最重要的数据结构

| 问题 | 首个对象 | 必须讲清 | 直接打开 |
|---|---|---|---|
| 整个 Run 状态放哪 | `AgentRunSession` | 跨 Turn live state | `agent_forge/runtime/application/session.py` |
| WorkingMemory 是什么 | `WorkingMemory` | Session 内 bounded task-state view | `agent_forge/runtime/application/working_memory.py` |
| 当前模型到底吃什么 | `PreparedTurn` | `turn_system_message + llm_messages + tool_schemas` | `agent_forge/runtime/application/turn_preparation.py` |
| 历史超限后是什么 | `ConversationHistoryDigest` | old Conversation projection，不是 Session dump | `agent_forge/context/domain/conversation_digest.py` |
| Crash 怎么继续 | `TaskCheckpoint` | execution snapshot + historical projection | `agent_forge/runtime/domain/task.py` |
| Tool 返回什么 | `Observation` | typed execution fact | `agent_forge/runtime/domain/conversation.py` |
| ToolCall 怎么治理 | `OperationIntent` | operation identity + side-effect semantics | `agent_forge/runtime/application/operation_tracker.py` |
| 最终证据怎么引用 | `EvidenceLedger` | final-answer grounding view | `agent_forge/runtime/application/evidence_ledger.py` |

---

# 3. 上下文与压缩 / Context & Compaction

首个文件：`agent_forge/context/application/compaction.py`。

```text
TurnPreparation.prepare_turn
↓
TurnSystemContextAssembler
↓
PromptWindowManager.prepare
↓
PreparedTurn
```

追：

```text
Session / WorkingMemory / Digest / Checkpoint
```

直接看：

- `Agent运行数据结构与模型输入.md`
- `上下文工程.md`
- `上下文压缩与长任务设计.md`

---

# 4. 工具治理 / Tool Governance

首个文件：`agent_forge/runtime/application/tool_execution.py`。

```text
ToolRouter.route
↓
ToolExecutionPipeline._execute_call
↓
OperationTracker.resolve_existing_operation
↓
ToolAuthorizationGate.authorize
↓
ToolRegistry.execute
↓
Observation
```

常见追问：

```text
Tool schema 为什么强约束？
→ Tool contract + runtime validation

Approval vs Ledger？
→ can execute vs already executed where

executing crash？
→ outcome unknown → fail closed
```

---

# 5. 持久化控制 / Durable Control

首个文件：`agent_forge/runtime/application/run_lifecycle.py`。

```text
RunLifecycle.update_checkpoint
↓
JsonTaskStateRepository

Human:
JsonHumanInputRepository

Approval:
JsonApprovalRepository

Side effect:
JsonOperationLedgerRepository
```

不要从 Trace 反推 mutable state。

---

# 6. 运行控制 / Run Control

首个文件：`agent_forge/runtime/application/run_control.py`。

```text
RunControlHandler.consume_pending_signals
```

重点：

```text
Pause / Cancel
→ safe boundary stop

Steer
→ user message + replan semantics
```

进程内 pending signal 不是 durable store。

---

# 7. Skill 能力

首个文件：`agent_forge/skills/registry.py`。

```text
RunPreparation._activate_skills
↓
SkillRegistry.select_for_task / activate
↓
Skill prompt card
```

当前要说清：

```text
metadata discovery
→ selected SKILL.md
→ bounded matched reference
```

当前 progressive disclosure 主要是 run-level，turn-level dynamic reference switching 是可演进项。

---

# 8. 多 Agent / Multi-Agent

首个文件：`agent_forge/multi_agent/application/live_fanout.py`。

```text
FanoutPlan
↓
build_conflict_free_batches
↓
LiveFanoutCoordinator.run
↓
LocalAgentWorkerAdapter.run_worker
↓
merge gates
↓
run_finalizer
```

当前 stable 是 post-planning execution half。

新的 capability branch 再看：

```text
Planner
WorkerHandoff
bounded recovery/replan
criteria-aware finalizer
```

---

# 9. 评测 / Evaluation

首个文件：`agent_forge/bench/application/swebench.py`。

```text
RunSwebench.run_benchmark
↓
Case Executor
↓
predictions
↓
official evaluator
↓
result / scorecard
```

实验结论必须区分：

```text
candidate patch
local validation
official resolved
infra invalid
```

不要把本地 compile/test 直接升格成 official pass。

---

# 10. 现场查代码原则

不要：

```text
grep 整个项目
→ 找 20 个类
→ 从 adapter/UI 开始读
```

要：

```text
先问“谁拥有这个业务判断？”
↓
打开 Application / Domain Owner
↓
再追 Port / Adapter
↓
最后用 test / trace 验证
```

这也是 NanoHarness 代码阅读最稳定的方式。
