# 历史实验归档

这里保存已经退出当前展示面、但仍值得保留审计证据的实验和预注册资产。平时不需要阅读，
当前入口也不会把它们作为产品能力或质量 headline。

`legacy-benchmarks/` 保留旧目录之间的相对位置，避免历史 README 和 manifest 完全失去上下文：

- `experiments/01—05`：早期 Runtime、Context、Ledger 与模型选型记录；
- `archive/`：quality-selection v1 的失败关闭机器记录；
- `showcase/`、`regression/`：已废弃的 Canonical-50、Golden 与模型选型预注册资产；
- `campaigns/`、`cohorts/`、`infrastructure/`：旧目录说明。

当前应阅读：

- [Tool / ACI R1](../experiments/tool-aci-r1/README.md)
- [Tool / ACI R2](../experiments/tool-aci-r2/README.md)
- [当前 Quality Showcase](../showcase/README.md)

归档资产不再拥有可运行的专用 runner；精确历史代码仍由 Git commit/tag 恢复。这样保留证据，
同时避免失效实现继续占用当前维护面。
